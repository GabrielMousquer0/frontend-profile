mutation SearchUsers($username: String!) {
    searchUsers(username: $username) {
        username
        id
        infos {
            role
            avatar
            description
            created_at
        }
        languages {
            name
            id
        }
    }
}